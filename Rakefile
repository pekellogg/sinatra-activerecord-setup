# grab: 
# session secret, gems, dev environment db, & app
# *app dir and subs should house Controller, Models, Views but do not in this example*
require './config/environment'

# grab rake tasks
require 'sinatra/activerecord/rake'